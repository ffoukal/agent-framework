# Next action

## Agent to use
<!-- Name the next agent AND resolve its model/effort from .agents/project/config.yml
     (models.agents[<agent>] -> tier/effort; models.mapping[tier][<cli>] -> model),
     e.g. "reviewer — model: opus, effort: high". The model shown is a snapshot at write
     time; config.yml is the single source of truth — if it changed, re-resolve from
     there. Advisory: the human (or orchestrator) picks the model. -->
## Instruction
## Read first
## Do
## Stop when
## Expected writes
